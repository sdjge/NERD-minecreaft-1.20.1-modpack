/*ItemEvents.modification(event => {
    const m_items = [{

    }]
    m_items.forEach((m_items) => {
        event.modify(m_items.item, item => {
            item.rarity = m_items.rarity
        });
    });
});*/
